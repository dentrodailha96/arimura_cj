name: SSH auth failure test

on: [push]

jobs:
  ssh-test:
    runs-on: ubuntu-latest
    steps:
      - name: Attempt SSH without a key
        run: |
          ssh \
            -o StrictHostKeyChecking=no \
            -o PasswordAuthentication=no \
            user@your-host.example.com
